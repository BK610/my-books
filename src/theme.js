import {DefaultTheme} from 'atomize';

export const theme = {
    ...DefaultTheme,
    fontFamily: {
        ...DefaultTheme.fontFamily,
        primary: "Cooper-Hewitt"
    },
    colors: {
        ...DefaultTheme.colors,
        inputGray: "#E9ECEF",
        brandNavy: "#29293E",
        brandTeal: "#5ABBC4"
    }
};