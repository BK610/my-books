const HomeSection = {
    height: '90vh',
    minHeight: "400px"
};

const Jumbotron = {
    backgroundColor: "#ffffff",
    height: "100%"
};

const Logo = {
    textAlign: 'center',
    maxWidth: '100%',
    maxHeight: "100vh",
};

export const styles = {
    HomeSection: HomeSection,
    Jumbotron: Jumbotron,
    Logo: Logo
};