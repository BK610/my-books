const Jumbotron = {
    backgroundColor: "#ffffff"
};

const Logo = {
    textAlign: 'center',
    maxWidth: '100%',
    maxHeight: "100vh",
};

export const styles = {
    Jumbotron: Jumbotron,
    Logo: Logo
};